name: Pre-deploy quality gate
description: Deprecated — deploy no longer runs a pre-deploy gate. Use PR Security quick check instead.

inputs:
  git_ref:
    description: Commit SHA or branch ref to validate
    required: true

runs:
  using: composite
  steps:
    - name: Skipped
      shell: bash
      run: |
        echo "Pre-deploy quality gate is disabled."
        echo "PR Security quick check + production environment approval are sufficient."

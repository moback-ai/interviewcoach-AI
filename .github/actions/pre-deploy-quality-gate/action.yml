name: Pre-deploy quality gate
description: Deprecated — use .github/actions/security-gate in deploy.yml and PR Security workflow.

inputs:
  git_ref:
    description: Commit SHA or branch ref to validate
    required: true

runs:
  using: composite
  steps:
    - name: Skipped
      shell: bash
      run: |
        echo "Use the Security gate job in Deploy · Production and PR security gate instead."

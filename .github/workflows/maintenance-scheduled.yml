name: Maintenance · Scheduled (disabled — use devsecops-platform)

on:
  workflow_dispatch:

jobs:
  blocked:
    runs-on: ubuntu-latest
    steps:
      - name: Maintenance moved to devsecops-platform
        run: |
          echo "::error::Run maintenance from moback-ai/devsecops-platform"
          exit 1

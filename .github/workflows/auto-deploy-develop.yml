name: Deploy · Auto (develop) (disabled)

on:
  workflow_dispatch:

jobs:
  blocked:
    runs-on: ubuntu-latest
    steps:
      - name: Auto-deploy moved to devsecops-platform
        run: |
          echo "::notice::Merged PRs no longer auto-deploy from this repo."
          echo "DevSecOps: moback-ai/devsecops-platform"
          exit 0

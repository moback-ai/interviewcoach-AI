# Retired — production deploy moved to moback-ai/devsecops-platform
name: Deploy · Production (retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: DevSecOps-only deploy
        run: |
          echo "This workflow is retired."
          echo "Deploy from: moback-ai/devsecops-platform → InterviewCoach · Deploy PROD"
          echo "Ask Govardhan or Kishore after your PR is merged to develop."
          exit 1

name: Deploy · Production (disabled — use devsecops-platform)

on:
  workflow_dispatch:

jobs:
  blocked:
    runs-on: ubuntu-latest
    steps:
      - name: Deploy moved to devsecops-platform
        run: |
          echo "::error::Production deploy is DevSecOps-only."
          echo "Use repo: moback-ai/devsecops-platform"
          echo "Workflow: InterviewCoach · Deploy Production"
          echo "Docs: docs/DEVSECOPS.md"
          exit 1

# Retired — production deploy moved to moback-ai/devsecops-platform
name: Deploy · Production (retired)

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  retired:
    runs-on: ubuntu-latest
    steps:
      - name: DevSecOps-only deploy
        run: |
          echo "This workflow is retired."
          echo "Deploy from: moback-ai/devsecops-platform"
          echo "  → InterviewCoach · Build Production"
          echo "  → InterviewCoach · Deploy Production"
          echo "Ask Govardhan or Kishore after your PR is merged to release/<month>-<year>."
          exit 1

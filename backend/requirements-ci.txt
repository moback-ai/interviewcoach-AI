# Slim deps for CI quality gate / pytest only (no ML stack).
# Production installs backend/requirements.txt on servers.
flask==3.1.3
flask-cors==6.0.0
werkzeug==3.1.6
pyjwt==2.13.0
bcrypt==4.2.1
requests==2.33.0
psycopg2-binary==2.9.10
boto3>=1.34,<2
pytest>=8.0.0
